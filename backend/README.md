# Backend

O backend da RASTREON fica em `server/`. Ele concentra a API Express, os eventos Socket.IO, autenticação, autorização, integrações e regras de negócio.

Os testes automatizados ficam em `tests/` e são executados a partir da raiz com `npm test`.
