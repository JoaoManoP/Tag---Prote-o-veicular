import React from 'react';import { FlatList,Text } from 'react-native';import { Card,EmptyState,Header,Screen,styles } from '../src/components/ui';import { api } from '../src/services/api';import { useApp } from '../src/state/AppContext';
export default function Notifications(){const{alerts,refresh,theme}=useApp();return <Screen scroll={false}><Header title="Notificações" subtitle="Segurança, rastreamento, viagem e sistema"/><FlatList data={alerts} keyExtractor={i=>i.id} contentContainerStyle={{gap:10}} ListEmptyComponent={<EmptyState title="Tudo tranquilo" message="Nenhum alerta foi recebido."/>} renderItem={({item})=><Card><Text style={{color:item.severity==='critical'?theme.colors.danger:theme.colors.text,fontWeight:'900'}} onPress={async()=>{if(!item.readAt){await api.patch(`/api/alerts/${item.id}/read`,{});refresh()}}}>{item.title}</Text><Text style={[styles.caption,{color:theme.colors.muted}]}>{new Date(item.occurredAt).toLocaleString('pt-BR')} · {item.type}</Text></Card>}/></Screen>}
