{
  "name": "rastro-demo-simulador",
  "version": "1.1.0",
  "private": true,
  "description": "RastroTack — plataforma demonstrativa de rastreamento e proteção veicular",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "lint": "node --check server/server.js && node --check server/auth.js && node --check server/authorization.js && node --check server/database.js && node --check server/migrations.js && node --check server/telemetry.js && node --check server/providers/route-provider.js && node --check server/providers/tracker-device-provider.js && node --check server/set-role.js && node --check public/js/dashboard.js && node --check public/js/auth.js && node --check public/js/mobile.js && node --check public/js/theme-init.js && node --check public/js/restricted-area.js",
    "build": "npm run lint",
    "db:init": "node server/init-database.js",
    "role:set": "node server/set-role.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^13.0.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^8.6.2",
    "express-session": "^1.19.0",
    "helmet": "^8.3.0",
    "leaflet": "^1.9.4",
    "qrcode": "^1.5.4",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
