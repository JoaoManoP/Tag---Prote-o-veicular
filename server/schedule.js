'use strict';

const WEEKDAYS = Object.freeze({ Sun: 0, Mon: 1, Tue: 2, Wed: 3, Thu: 4, Fri: 5, Sat: 6 });
function timeToMinutes(value) { if (!/^([01]\d|2[0-3]):[0-5]\d$/.test(String(value))) return null; const [hours, minutes] = value.split(':').map(Number); return hours * 60 + minutes; }
function validateSchedule(value) { if (!value || typeof value !== 'object') return null; const from = String(value.from || ''), to = String(value.to || ''), fromMinutes = timeToMinutes(from), toMinutes = timeToMinutes(to); const days = Array.isArray(value.days) ? [...new Set(value.days.map(Number))].filter(day => Number.isInteger(day) && day >= 0 && day <= 6).sort() : []; const timezone = typeof value.timezone === 'string' && value.timezone.length <= 80 ? value.timezone : 'America/Sao_Paulo'; try { new Intl.DateTimeFormat('en-US', { timeZone: timezone }).format(); } catch { return null; } if (fromMinutes == null || toMinutes == null || !days.length) return null; return { enabled: value.enabled !== false, days, from, to, timezone }; }
function localParts(timestamp, timezone) { const parts = new Intl.DateTimeFormat('en-US', { timeZone: timezone, weekday: 'short', hour: '2-digit', minute: '2-digit', hourCycle: 'h23' }).formatToParts(new Date(timestamp)); const values = Object.fromEntries(parts.map(part => [part.type, part.value])); return { weekday: WEEKDAYS[values.weekday], minutes: Number(values.hour) * 60 + Number(values.minute) }; }
function isWithinSchedule(schedule, timestamp = Date.now()) { if (!schedule?.enabled) return true; const normalized = validateSchedule(schedule); if (!normalized) return false; const local = localParts(timestamp, normalized.timezone), from = timeToMinutes(normalized.from), to = timeToMinutes(normalized.to); if (from <= to) return normalized.days.includes(local.weekday) && local.minutes >= from && local.minutes <= to; if (normalized.days.includes(local.weekday) && local.minutes >= from) return true; const previousDay = (local.weekday + 6) % 7; return normalized.days.includes(previousDay) && local.minutes <= to; }
module.exports = { validateSchedule, isWithinSchedule, timeToMinutes };
