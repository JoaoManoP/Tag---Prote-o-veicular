/* global io, OfflinePositionQueue */
'use strict';
const $ = id => document.getElementById(id);
const invite = new URLSearchParams(location.hash.slice(1) || location.search);
const invitedSessionId = invite.get('session'), invitedToken = invite.get('token');
if (invitedSessionId && invitedToken) { sessionStorage.setItem('rastreon-mobile-session', invitedSessionId); sessionStorage.setItem('rastreon-mobile-token', invitedToken); history.replaceState(null, '', '/mobile.html'); }
let sessionId = invitedSessionId || sessionStorage.getItem('rastreon-mobile-session'), mobileToken = invitedToken || sessionStorage.getItem('rastreon-mobile-token');
const socket = io({ reconnection: true, reconnectionDelay: 800, reconnectionDelayMax: 5000 });
let queue = sessionId ? new OfflinePositionQueue(sessionId) : null;
const deviceId = sessionStorage.getItem('rastreon-mobile-device') || `web-${globalThis.crypto?.randomUUID?.() || Math.random().toString(36).slice(2)}`;sessionStorage.setItem('rastreon-mobile-device',deviceId);
let watchId = null, sharing = false, count = 0, distance = 0, lastPosition = null, sessionValid = false, sequence = Date.now() * 1000, lostAt = null, sendingQueue = false, consentGranted = false, consentPending = null;

function toast(message) { $('toast').textContent = message; $('toast').classList.add('show'); setTimeout(() => $('toast').classList.remove('show'), 2800); }
function status(text, online = false) { $('mobileStatus').className = `badge ${online ? 'online' : 'offline'}`; $('mobileStatus').textContent = `● ${text}`; $('mConnection').textContent = text; }
function haversine(a, b) { const radius = 6371000, radians = value => value * Math.PI / 180, d1 = radians(b.latitude - a.latitude), d2 = radians(b.longitude - a.longitude), value = Math.sin(d1 / 2) ** 2 + Math.cos(radians(a.latitude)) * Math.cos(radians(b.latitude)) * Math.sin(d2 / 2) ** 2; return 2 * radius * Math.asin(Math.sqrt(value)); }
async function updateQueueCount() { const total = queue ? await queue.count() : 0; $('mQueue').textContent = `${total} ponto${total === 1 ? '' : 's'}`; return total; }
async function enqueue(position) { if (!queue) return; await queue.add({ ...position, capturedOffline: true }); await updateQueueCount(); }
function stop(message = 'Compartilhamento parado.') { if (watchId !== null) navigator.geolocation.clearWatch(watchId); if(consentGranted&&socket.connected)socket.emit('consent:revoke',{deviceId},()=>{});consentGranted=false;consentPending=null;watchId = null; sharing = false; $('startBtn').classList.remove('hidden'); $('startBtn').disabled = !sessionValid; $('stopBtn').classList.add('hidden'); status(sessionValid ? 'Pronto para compartilhar' : 'Sessão indisponível', sessionValid); toast(message); }
function render(position) { if (lastPosition) { const step = haversine(lastPosition, position); if (step < 2000) distance += step; } lastPosition = position; $('mLatitude').textContent = position.latitude.toFixed(6); $('mLongitude').textContent = position.longitude.toFixed(6); $('mAccuracy').textContent = `${position.accuracy.toFixed(1)} m`; $('mSpeed').textContent = position.speed == null ? 'Não disponível' : `${(Math.max(0, position.speed) * 3.6).toFixed(1)} km/h`; $('mUpdated').textContent = new Date(position.timestamp).toLocaleString('pt-BR'); $('mDistance').textContent = distance < 1000 ? `${distance.toFixed(0)} m` : `${(distance / 1000).toFixed(2)} km`; }
function grantConsent(){if(consentGranted)return Promise.resolve(true);if(consentPending)return consentPending;consentPending=new Promise(resolve=>socket.emit('consent:grant',{deviceId,purpose:'vehicle-tracking'},result=>{consentGranted=Boolean(result?.ok);consentPending=null;resolve(consentGranted)}));return consentPending}
async function onPosition(result) { const coords = result.coords, position = { deviceId, latitude: coords.latitude, longitude: coords.longitude, accuracy: coords.accuracy, speed: coords.speed, heading: coords.heading, altitude: coords.altitude, timestamp: result.timestamp, source: 'mobile-gps', sequence: ++sequence }; render(position); if (!socket.connected || !sessionValid) { if (!lostAt) lostAt = Date.now(); await enqueue(position); status('Sem internet · GPS salvo no aparelho'); return; }if(!await grantConsent()){await enqueue(position);return toast('Não foi possível registrar o consentimento.')} socket.emit('position:update', position, async acknowledgement => { if (!acknowledgement?.ok) { if (acknowledgement?.error?.includes('encerrada')) return stop(acknowledgement.error); if (!lostAt) lostAt = Date.now(); await enqueue(position); return; } if (acknowledgement.accepted) count++; $('mCount').textContent = count; status('Compartilhando localização', true); }); }
function onError(error) { const messages = { 1: 'Permissão de localização negada.', 2: 'Localização indisponível.', 3: 'Tempo esgotado ao obter localização.' }; toast(messages[error.code] || 'Erro ao acessar a localização.'); if (error.code === 1) stop('A permissão foi negada. Nada foi compartilhado.'); }
function start() { if (!sessionValid || sharing) return; if(!window.isSecureContext&&location.hostname!=='localhost')return toast('O GPS do navegador exige uma conexão HTTPS.');if (!navigator.geolocation) return toast('Este navegador não oferece geolocalização.'); sharing = true; $('startBtn').classList.add('hidden'); $('stopBtn').classList.remove('hidden'); status('Solicitando autorização…'); watchId = navigator.geolocation.watchPosition(onPosition, onError, { enableHighAccuracy: true, maximumAge: 0, timeout: 15000 }); }
async function flushQueue() { if (!queue || sendingQueue || !sessionValid || !socket.connected) return; const batch = await queue.list(200); if (!batch.length) { await updateQueueCount(); return; } sendingQueue = true; status(`Enviando lote de ${batch.length} ponto(s)…`); socket.emit('positions:batch', { points: batch, lostAt }, async result => { sendingQueue = false; if (!result?.ok) return status(result?.error || 'Falha ao sincronizar'); await queue.removeSequences(result.confirmedSequences || []); count += result.received || 0; $('mCount').textContent = count; const remaining = await updateQueueCount(); if (!remaining) { lostAt = null; toast('Fila offline confirmada pelo servidor.'); status(sharing ? 'Compartilhando localização' : 'Pronto para compartilhar', true); } else flushQueue(); }); }
function join() { if (!sessionId || !mobileToken) return status('Link de sessão inválido'); socket.emit('session:join', { sessionId, role: 'mobile', token: mobileToken }, result => { sessionValid = Boolean(result?.ok);consentGranted=false;$('startBtn').disabled = !sessionValid; if (sessionValid) { const vehicle = result.session.vehicle; $('mobileVehicle').classList.remove('empty'); $('mobileVehicle').textContent = vehicle ? `${vehicle.nickname} — ${vehicle.brand} ${vehicle.model} ${vehicle.version}` : 'Veículo não informado'; status(sharing ? 'Compartilhando localização' : 'Pronto para compartilhar', true);if(sharing)grantConsent().then(flushQueue); } else { status('Sessão inválida ou encerrada'); if (sharing) stop('A sessão não está mais disponível.'); } }); }

async function pair(event){event.preventDefault();const code=$('pairingCode').value.toUpperCase().replace(/[^A-Z0-9]/g,'');if(code.length!==8)return toast('Digite o código de 8 caracteres.');const button=$('pairingForm').querySelector('button');button.disabled=true;try{const response=await fetch('/api/mobile/pair',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})}),data=await response.json();if(!response.ok)throw new Error(data.error);sessionId=data.sessionId;mobileToken=data.token;sessionStorage.setItem('rastreon-mobile-session',sessionId);sessionStorage.setItem('rastreon-mobile-token',mobileToken);queue=new OfflinePositionQueue(sessionId);$('pairingForm').classList.add('hidden');join();await updateQueueCount()}catch(error){toast(error.message);button.disabled=false}}

$('startBtn').onclick = start;
$('stopBtn').onclick = () => stop();
$('pairingForm').onsubmit=pair;
if(!sessionId||!mobileToken){$('pairingForm').classList.remove('hidden');status('Digite o código do painel')}
socket.on('connect', join);
socket.on('disconnect', () => { if (sharing && !lostAt) lostAt = Date.now(); status(sharing ? 'Sem internet · armazenando GPS no IndexedDB' : 'Internet desconectada; tentando reconectar…'); });
socket.on('session:closed', async () => { sessionValid = false; sessionStorage.removeItem('rastreon-mobile-session'); sessionStorage.removeItem('rastreon-mobile-token'); if (queue) await queue.clear(); await updateQueueCount(); stop('O painel encerrou esta sessão.'); status('Sessão encerrada'); });
updateQueueCount().catch(() => status('Armazenamento offline indisponível'));
